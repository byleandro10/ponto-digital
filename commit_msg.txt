fix: corrige geofence, selfie, timezone e adiciona historico do funcionario

- fix(geofence): aceita radiusMeters ou radius no payload
- fix(geofence): normaliza retorno com radiusMeters para exibicao correta
- fix(geofence): parseFloat/parseInt explicito nas coordenadas e raio
- fix(auth): rota GET /geofences/config aceita token de admin e funcionario
- fix(timezone): utilitario brazilTime.js para horario correto (America/Sao_Paulo)
- fix(timeEntry): clockPunch, getTodayEntries, getHistory usam fuso BR
- feat(employee): pagina MyHistory.jsx com historico de pontos somente leitura
- feat(employee): rota /employee/history no App.jsx
- feat(employee): botao Historico no header do PunchClock
